document.addEventListener('DOMContentLoaded',async()=>{const db=window.forumAuth,session=(await db.auth.getSession()).data.session;if(!session)return location.replace('auth.html?next=admin');const esc=v=>String(v||'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const load=async()=>{const r=await fetch('/api/admin-users',{headers:{authorization:`Bearer ${session.access_token}`}});if(!r.ok){document.body.innerHTML='<main class="forum"><h1>无主管理员权限</h1></main>';return}const users=await r.json();document.querySelector('[data-count]').textContent=users.length;document.querySelector('[data-users]').innerHTML=users.map(u=>`<tr><td>${esc(u.username)}</td><td>${esc(u.email)}</td><td>${new Date(u.created_at).toLocaleString('zh-CN')}</td><td>${esc(u.role)}</td><td>${esc(u.registration_audit?.province)}</td><td>${esc(u.registration_audit?.device_name)}</td><td>${u.role==='admin'?'主管理员':`<button data-id="${u.id}" data-role="${u.role==='superadmin'?'member':'superadmin'}">${u.role==='superadmin'?'撤销超管':'设为超管'}</button>`}</td></tr>`).join('')};document.addEventListener('click',async e=>{const id=e.target.dataset.id;if(!id)return;const r=await fetch('/api/admin-users',{method:'PATCH',headers:{authorization:`Bearer ${session.access_token}`,'content-type':'application/json'},body:JSON.stringify({id,role:e.target.dataset.role})});if(!r.ok)return alert('操作失败');load()});load()});
