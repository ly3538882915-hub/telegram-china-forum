const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'content-type':'application/json; charset=utf-8'}});export async function onRequest({request,env}){try{const token=request.headers.get('authorization')||'';const r=await fetch(`${env.SUPABASE_URL}/auth/v1/user`,{headers:{authorization:token,apikey:env.SUPABASE_SERVICE_ROLE_KEY}});if(!r.ok)return json({error:'unauthorized'},401);const user=await r.json();if(request.method==='DELETE'){const body=await request.json();if(body.confirmation!=='DELETE')return json({error:'confirmation_required'},400);const d=await fetch(`${env.SUPABASE_URL}/auth/v1/admin/users/${user.id}`,{method:'DELETE',headers:{authorization:`Bearer ${env.SUPABASE_SERVICE_ROLE_KEY}`,apikey:env.SUPABASE_SERVICE_ROLE_KEY}});return json({ok:d.ok},d.ok?200:500);}return json({error:'method'},405)}catch{return json({error:'server_error'},500)}}
