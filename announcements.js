document.addEventListener('DOMContentLoaded',async()=>{const db=window.forumAuth;if(!db)return;const box=document.querySelector('[data-post-list]');const{data,error}=await db.from('posts').select('*,profiles(username),post_media(*)').eq('official',true).order('created_at',{ascending:false});if(error)return box.textContent='加载失败。';box.innerHTML=data.length?data.map(p=>`<article class="post"><b class="official">官方公告</b><h2>${p.profiles?.username||'管理员'}</h2><p class="post-meta">${new Date(p.created_at).toLocaleString('zh-CN')}</p><div class="post-body">${p.body.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]))}</div></article>`).join(''):'<p class="forum-note">暂无官方公告。</p>';});
