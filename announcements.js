document.addEventListener('DOMContentLoaded',async()=>{const db=window.forumAuth,box=document.querySelector('[data-post-list]');if(!db)return;const{data,error}=await db.from('posts').select('*,post_media(*)').eq('official',true).order('created_at',{ascending:false});if(error){box.textContent=`加载失败：${error.message}`;return}const esc=v=>String(v||'').replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));box.innerHTML=data.length?data.map(p=>`<article class="post"><b class="official">论坛官方认证 ✓</b><h2>${esc(p.author_name||'论坛官方')}</h2>${p.show_timestamp?`<p class="post-meta">${new Date(p.created_at).toLocaleString('zh-CN')}</p>`:''}<div class="post-body">${esc(p.body)}</div><div class="post-media">${(p.post_media||[]).map(m=>m.mime_type.startsWith('image/')?`<img src="${db.storage.from('forum-media').getPublicUrl(m.path).data.publicUrl}">`:`<audio controls src="${db.storage.from('forum-media').getPublicUrl(m.path).data.publicUrl}"></audio>`).join('')}</div><p><a href="post.html?id=${p.id}">${p.comment_enabled?'进入公告、点赞与评论':'查看公告、点赞与收藏'}</a></p></article>`).join(''):'<p class="forum-note">暂无官方公告。</p>'});
